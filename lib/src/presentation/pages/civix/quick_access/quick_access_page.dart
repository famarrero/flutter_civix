import 'package:flutter/material.dart';

class QuickAccessPage extends StatelessWidget {
  const QuickAccessPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Quick access'),),
    );
  }
}
