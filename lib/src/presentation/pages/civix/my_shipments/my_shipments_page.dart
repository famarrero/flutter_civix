import 'package:flutter/material.dart';

class MyShipmentsPage extends StatelessWidget {
  const MyShipmentsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('My shipments'),),
    );
  }
}